# 雷石 K 歌智能助手 - 开发总结

## 主要困难与解决方案

### 困难 1: 向量数据库选择问题 - Milvus Lite 兼容性差

**问题描述**:
- 最初选择了 Milvus + Milvus Lite 作为向量数据库
- 但 Milvus Lite 在 Windows/PowerShell 环境下安装和配置非常困难
- `pip install pymilvus[milvus_lite]` 经常失败，且与 Python 版本兼容性差
- 即使安装成功，在代码中调用时也会出现 `ModuleNotFoundError: No module named 'milvus_lite'`

**解决方案**:
- **方案 A（推荐）**: 完全移除 Milvus，改用本地 FAISS + NumPy 手动计算
  - 优点：无需外部数据库服务，100% 纯 Python，兼容性好
  - 缺点：小规模数据下性能尚可，大规模数据不如专业库
  - 最终选择此方案

- **方案 B**: 使用 ChromaDB，但同样有依赖问题
- **方案 C**: 使用纯本地向量存储（如 JSON + 手动计算），也在考虑范围

**最终实现**: 纯 FAISS + JSON 持久化，完美解决问题

---

### 困难 2: Hugging Face 模型下载需要翻墙

**问题描述**:
- 最初使用 HuggingFace 的 `sentence-transformers` 加载 BGE 模型
- 在中国大陆无法直接访问 HuggingFace，导致模型下载超时或失败
- 即使能下载，速度也非常慢，不适合项目要求

**解决方案**:
- 改用 ModelScope（阿里达摩院的国内平台）加载 BGE 模型
- `modelscope` 库在国内访问速度快，无需翻墙
- 添加降级方案：如果 ModelScope 也下载失败，使用简单字符级向量，保证系统正常运行

**代码改进**:
```python
# 原方案（需要翻墙）
from sentence_transformers import SentenceTransformer
model = SentenceTransformer('BAAI/bge-small-zh-v1.5')

# 新方案（国内直接访问）
try:
    from modelscope.pipelines import pipeline
    from modelscope.utils.constant import Tasks
    # 使用 ModelScope 加载
except Exception:
    # 降级到简单字符向量
    pass
```

---

### 困难 3: Pickle 序列化导致的类路径问题

**问题描述**:
- 使用 Pickle 持久化 FAISS Document 对象
- 当代码路径改变（如 `milvus_db/` → `vector_db/`）或模块结构调整后，Pickle 无法反序列化对象
- 报错：`AttributeError: Can't get attribute 'FAISSDocument' on <module '__main__'>`

**解决方案**:
- 完全放弃 Pickle，改用 JSON 格式存储向量和文档
- 自定义对象转字典，numpy 数组转 list 的转换
- 简单、可靠、兼容性好，且便于调试

**代码改进**:
```python
# 保存时
doc_dict = {
    "id": doc["id"],
    "text": doc["text"],
    "metadata": doc["metadata"],
    "vector": doc["vector"].tolist()  # numpy → list
}
json.dump(doc_dict, file)

# 加载时
doc_dict = json.load(file)
doc_dict["vector"] = np.array(doc_dict["vector"])  # list → numpy
```

---

### 困难 4: PowerShell 脚本执行限制

**问题描述**:
- Windows PowerShell 默认有执行策略限制，禁止运行未签名脚本
- 即使绕过，PowerShell 5 的语法和 PowerShell 7/Core 也有差异
- 容易出现 `ParseException` 等语法错误

**解决方案**:
- 创建 Python 启动脚本 `setup_and_run.py`，完全避免 PowerShell 脚本问题
- Python 脚本跨平台，兼容性最好，语法清晰
- 保留原始 PowerShell 脚本作为备份，但不再推荐使用

---

### 困难 5: 前端界面美观度与交互性

**问题描述**:
- 原始 Streamlit 界面过于简单朴素
- 用户体验不佳，缺少专业感和美观度

**解决方案**:
- 使用自定义 CSS 美化界面
- 添加渐变背景、圆角卡片、阴影效果
- 侧边栏快速问题按钮、功能卡片展示
- 聊天界面优化，头像、消息气泡样式
- 参考现代网页设计风格，让界面更专业

---

## 最终技术方案

| 组件 | 最初选择 | 最终选择 | 理由 |
|------|---------|---------|------|
| 向量数据库 | Milvus Lite | FAISS + JSON | 简单、无依赖、兼容性好 |
| Embedding 源 | HuggingFace | ModelScope + 降级方案 | 国内可用，稳定 |
| LLM | 智谱 AI | 智谱 AI | 无需翻墙，效果好 |
| Web 框架 | Streamlit | Streamlit + 自定义 CSS | 快速开发 + 美观 |
| 数据持久化 | Pickle | JSON | 简单、可靠、兼容性好 |
| 启动脚本 | PowerShell | Python | 跨平台，无限制 |

## 关键经验

1. **在中国大陆开发 AI 项目，技术选型要优先考虑国内可用的服务**
   - Embedding 优先考虑 ModelScope 或魔搭社区
   - LLM 优先考虑智谱、通义千问、文心一言等
   
2. **向量数据库在小规模数据下，不一定要用复杂的库**
   - FAISS + NumPy + JSON 对于 1000 条以内的数据完全够用
   - 开发效率和可维护性最重要

3. **慎用 Pickle 做持久化**
   - 对于跨版本、跨环境运行的项目，Pickle 的兼容性问题多
   - JSON 或 MessagePack 是更好的选择

4. **优先使用 Python 做自动化脚本**
   - 跨平台，无执行策略问题，语法统一
